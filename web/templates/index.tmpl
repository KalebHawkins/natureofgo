<!DOCTYPE html>
</html>
    <head></head>
    <body>
        <h1>Welcome to Nature Of Go</h1>
        {{range .Examples}}
        <h2>{{.Title}}</h2>
        <iframe src="/run?wasm={{.WasmFile}}" width="640" height="240"></iframe>
        <p>{{ .Description }}</p>
        {{end}}
    </body>
</html>